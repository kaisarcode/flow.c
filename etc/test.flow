flow.link=root

func.hello.exec=Hello <arg.name>

# Call func inside a data key
node.data.name=World
node.data.msg=<func.hello data>

# Use that resolved data key in exec
node.root.exec=printf "Message: <node.data.msg>"
